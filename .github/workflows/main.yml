name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: my-step
        run: |
          echo hello ken
          echo hello sloat
          pwd
          ls
          git log
